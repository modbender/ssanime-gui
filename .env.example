# Local test configuration. Copy to .env (gitignored) and fill in.
# Do NOT commit real extension/repo URLs — see CLAUDE.md "Legal" section.

# Repo index URL for the live extension integration test
# (go test -tags live ./internal/extension/...). The test skips when unset.
SSANIME_TEST_REPO_INDEX=
