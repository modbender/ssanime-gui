// @fontsource packages are side-effect CSS imports that ship no type
// declarations; declare the module so `svelte-check`/`tsc` accept the import.
declare module '@fontsource-variable/plus-jakarta-sans'
